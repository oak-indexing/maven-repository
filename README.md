# oak-indexing Maven Repository

Public Maven repository for oak-indexing artifacts.

## Usage

Add to your project `pom.xml`:

```xml
<repository>
  <id>oak-indexing</id>
  <url>https://oak-indexing.github.io/maven-repository/</url>
</repository>
```

